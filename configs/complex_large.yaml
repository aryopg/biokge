dataset_configs:
  dataset_name: "ogbl-ppa"
  datasets_dir: null
model_configs:
  model_type: "complex"
  hidden_size: 500
  regularizers:
    - type: n3
      coeff: 0.01
    - type: f2
      coeff: 0
  dropout: 0.0
  batch_size: 50
  optimizer: "adagrad"
  learning_rate: 0.1
  loss_fn: "crossentropy"
  grad_accumulation_step: 10
training_configs:
  epochs: 40
  eval_steps: 2
  device: 0
  random_seed: 1234
  outputs_dir: "outputs"